# 课程设计报告修改清单

只列出**需新增**和**必须修改**的部分，已有内容不动。

---

## 一、功能模块 → 新增

**位置**：在"（6）购物车模块"之后，另起段落添加

### （7）AI 智能助手模块
集成 DeepSeek 大语言模型，提供智能导购对话服务。支持新建对话、多轮上下文对话、历史对话记录管理。助手可根据对话上下文推荐商品。涉及 AiChatPage（前端）+ AiChatController/AiChatService（后端），数据表为 vmall_conversations 和 vmall_conversation_messages。

---

## 二、关键页面视图设计 → 新增

**位置**：在"（5）我的"之后，依次添加

### （6）登录页
顶部展示华为商城 Logo 和名称，中间为登录表单（手机号 + 密码/验证码），支持切换密码登录和验证码登录两种模式，底部提供注册入口。路由：pages/LoginPage。

### （7）注册页
顶部导航栏含返回按钮和"注册"标题，输入项包括用户名、手机号、密码、验证码。点击"获取验证码"触发后端发送验证码，点击"注册"提交信息后自动登录跳转首页。路由：pages/RegisterPage。

### （8）商品详情页
顶部返回按钮，居中展示商品大图，下方展示商品名称、价格和描述，底部固定"加入购物车"按钮。支持从首页和分类页的商品入口携带参数跳转。路由：pages/ProductDetailPage。

### （9）AI 智能助手页
顶部返回按钮和"智能导购助手"标题，对话区域以气泡形式展示用户和助手消息，底部为输入框和发送按钮。进入页面自动创建或加载最近对话。路由：pages/AiChatPage。

---

## 三、核心业务流程 → 修改

### 流程二：商品浏览与搜索 → 改为"商品浏览"，删除搜索相关描述

**位置**：现有标题"流程二：商品浏览与搜索"，改为"流程二：商品浏览"，段落中删除"用户点击商品卡片可进入搜索详情页"等搜索文案。

---

## 四、核心业务流程 → 新增

**位置**：在"流程五：购物车管理"之后，依次添加

### 流程六：首页数据加载
进入首页 Tab → HomePage 发起请求 GET /api/home → 后端聚合首页分类、活动、推荐商品数据 → 前端渲染搜索栏、16 宫格分类入口、轮播图、活动入口、水平商品推荐列表。

### 流程七：发现页内容加载
点击发现 Tab → GET /api/discover → 后端按"页面 → Tab → 区块 → 内容项"四级结构组装数据 → 前端渲染轮播广告、Tab 栏（推荐/发布会/选购攻略/玩机技巧）、各 Tab 下内容卡片列表。

### 流程八：AI 智能导购对话
点击 AI 悬浮按钮跳转 AiChatPage → 加载对话列表和最近消息 → 用户发送消息 → POST /api/ai/chat → 后端查对话上下文 → 调 DeepSeek API → 流式返回 AI 回复 → 同时保存消息到数据库 → 前端逐步渲染。

---

## 五、关键接口设计 → 新增

**位置**：该部分在"核心数据表结构"之前。检查现有表格，补充以下缺失接口：

### 新增表格：AI 对话接口

| 接口路径 | 方法 | 说明 |
|---------|------|------|
| /api/ai/chat | POST | AI 对话（携带对话 ID 和消息内容，返回 AI 流式回复） |
| /api/conversations | GET | 获取当前用户对话列表 |
| /api/conversations/{id}/messages | GET | 获取指定对话的消息列表 |

### 新增表格：首页接口

| 接口路径 | 方法 | 说明 |
|---------|------|------|
| /api/home | GET | 聚合首页数据（分类、活动、推荐商品） |
| /api/activities | GET | 活动入口列表 |

### 补充字段：购物车接口（修改现有表格）

| 接口路径 | 方法 | 补充说明 |
|---------|------|---------|
| PUT /api/cart/items/{id} | PUT | 更新数量或选中状态（若数量传 0 则删除） |
| PUT /api/cart/select-all | PUT | 全选/取消全选 |

---

## 六、核心数据表结构 → 修改与新增

**位置**：该部分已有表格基础上，删除购物车表多余字段，并追加 7 张新表。

### 购物车表（vmall_cart_items）→ 删除以下两行
- quantity（该字段在最终实现中已被移除）
- selected（该字段在最终实现中已被移除）

### 在"发现页内容表"之后追加以下 7 张表

1．**活动表（vmall_homepage_activities）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | 主键 | 活动ID |
| title | TEXT | NULL | 活动标题 |
| activity_url | TEXT | NULL | 活动链接 |
| scraped_at | DATETIME | NULL | 数据抓取时间 |

2．**搜索建议表（vmall_homepage_search_suggestions）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | 主键 | 建议项ID |
| keyword | VARCHAR(255) | NULL | 搜索关键词 |
| scraped_at | DATETIME | NULL | 数据抓取时间 |

3．**AI 对话表（vmall_conversations）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | 主键，自增 | 对话ID |
| user_id | BIGINT | NULL | 用户ID，匿名对话为NULL |
| title | VARCHAR(200) | DEFAULT '新对话' | 对话标题 |
| status | TINYINT | DEFAULT 1 | 状态：1=活跃 0=归档 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

4．**AI 对话消息表（vmall_conversation_messages）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | 主键，自增 | 消息ID |
| conversation_id | BIGINT | NOT NULL | 所属对话ID |
| role | VARCHAR(20) | NOT NULL | 角色：user/assistant |
| content | TEXT | NOT NULL | 消息内容 |
| metadata | TEXT | NULL | JSON格式扩展信息，如推荐商品列表 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

5．**发现页页面表（vmall_discover_pages）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | 主键，自增 | 页面ID |
| page_id | VARCHAR(32) | UNIQUE NOT NULL | 页面标识 |
| page_name | VARCHAR(64) | NOT NULL | 页面名称 |
| source_url | VARCHAR(500) | NULL | 页面来源URL |
| sort_order | INT | NOT NULL DEFAULT 0 | 排序序号 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

6．**发现页 Tab 表（vmall_discover_tabs）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | 主键，自增 | Tab ID |
| tab_key | VARCHAR(64) | UNIQUE NOT NULL | Tab标识键 |
| tab_name | VARCHAR(64) | NOT NULL | Tab显示名称 |
| related_page_id | VARCHAR(32) | NOT NULL | 关联页面ID |
| sort_order | INT | NOT NULL DEFAULT 0 | 排序序号 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

7．**发现页导航图标表（vmall_discover_nav_icons）**

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | 主键，自增 | 图标ID |
| page_id | VARCHAR(32) | NOT NULL | 所属页面ID |
| position | VARCHAR(16) | NOT NULL | 位置：left/right |
| title | VARCHAR(128) | NOT NULL | 图标标题 |
| icon_attribute | VARCHAR(64) | NULL | 图标属性标识 |
| image_url | VARCHAR(1000) | NULL | 图标图片URL |
| link_url | VARCHAR(1000) | NULL | 点击跳转链接 |
| sort_order | INT | NOT NULL DEFAULT 0 | 排序序号 |
| created_at | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
